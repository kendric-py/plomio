# packages/api_keys

> Заглушка. Доменная область ещё не проработана.

Доменная область API-ключей пользователя. Должна описывать выпуск, отзыв, срок действия и права
доступа API-ключей, используемых для прямого доступа к API (в обход фронтенда).

Сейчас содержит только минимальную модель `ApiKey` (`id`, `user_id`), достаточную для связи
`User.api_keys` из [`packages/user`](../user/AGENTS.md).

> TODO: определить формат ключа, хранение (хэш/сырое значение), срок действия, права/скоупы, отзыв.
