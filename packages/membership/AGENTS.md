# packages/membership

> Заглушка. Доменная область ещё не проработана.

Доменная область подписок/тарифов (membership) пользователя. Должна описывать, какой тариф у
пользователя действует, историю подписок и условия признания подписки активной.

Сейчас содержит только минимальную модель `Membership` (`id`, `user_id`), достаточную для связи
`User.memberships` из [`packages/user`](../user/AGENTS.md).

> TODO: определить состав тарифа/подписки, срок действия, статус (активна/истекла/отменена) — см.
> раздел «Домен: пользователи и тарифы» в корневом [`AGENTS.md`](../../AGENTS.md).
